{
  "name": "NCore.Optional",
  "version": "1.2.0",
  "description": "Optional type for C#",
  "main": "index.js",
  "files": [
    "lib",
    "scripts"
  ],
  "scripts": {
    "test": "dotnet build && cd NCore.Optional.Test && dotnet test",
    "build": "rm -f lib/* && dotnet clean && dotnet build && cd NCore.Optional && dotnet pack -c Release --include-symbols --include-source -o ../lib",
    "postinstall": "node scripts/postinstall.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
  }
}
